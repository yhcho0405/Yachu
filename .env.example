# Production runtime: SESSION_SECRET (configured only by GitHub Actions).
# Deployment step only: CLOUDFLARE_API_TOKEN, CLOUDFLARE_ACCOUNT_ID.
# npm run dev generates a separate random, git-ignored .dev.vars automatically.
